<!DOCTYPE html>
<html>
  <head>
    <title>Bonafide Certificate</title>
    <style>
      @page {
        margin: 2.54cm;
      }
      body {
        font-family: 'Times New Roman', serif;
        margin: 0;
        padding: 0;
        background: white;
      }
      .certificate {
        width: 100%;
        margin: auto;
        line-height: 1.6;
        font-size: 18px;
        position: relative;
        box-sizing: border-box;
        min-height: 100%;
      }
      .header {
        display: grid;
        grid-template-columns: 90px auto;
        align-items: center;
        margin-bottom: 5px;
      }
      .logo {
        width: 70px;
        height: auto;
      }
      .header-center {
        text-align: center;
      }
      .header-center h3 {
        font-size: 16px;
        margin-top: 10px;
        margin-left: 10px;
      }
      .header-center p {
        font-size: 13px;
        font-weight: 600;
        position: relative;
        top: -30px;
        margin-bottom: 0;
      }
      .header-line {
        border-top: 2px solid black;
        margin: 10px 0 20px;
      }

      h3 {
        text-align: center;
        font-size: 15px;
        font-weight: bold;
        margin: 35px 0;
      }
      .row {
        display: flex;
        justify-content: space-between;
        margin: 20px 0;
        font-size: 16px;
      }
      .body-text {
        text-align: justify;
        font-size: 19px;
        font-weight: 500;
        margin-top: 20px;
        text-indent: 50px;
        letter-spacing: 0.5px;
        line-height: 2;
      }
      .signature {
        margin-top: 350px;
        text-align: right;
        font-weight: bold;
        font-size: 18px;
      }

      .footer-line {
        border-top: 1px solid black;
        margin-top: 20px;
      }
      .footer {
        text-align: center;
        font-size: 14px;
        margin-top: 5px;
      }
    </style>
  </head>
  <body>
    <div class="certificate">
      <div class="header">
        <img src="http://localhost:3000/images/tce_logo_1.png" class="logo" />
        <div class="header-center">
          <h3>THIAGARAJAR COLLEGE OF ENGINEERING, MADURAI - 625015</h3>
          <p>
            (A Govt.Aided Autonomous Institution affiliated to Anna University)
          </p>
        </div>
      </div>

      <div class="header-line"></div>

      <div class="row">
        <p>No.D/2025</p>
        <p>Date: <%= String(formData.date) %></p>
      </div>
      <h3>BONAFIDE CERTIFICATE</h3>

      <p class="body-text">
        Certified that
        <strong
          ><%= String(formData.title) %> <%= String(formData.name) %></strong
        >, (Roll No: <strong><%= String(formData.rollno) %></strong>)
        <strong
          ><%= String(formData.relation) %> <%= String(formData.parentName)
          %></strong
        >
        is a bonafide student of this College studying in
        <strong
          ><%= String(formData.year) %> Year, <%= String(formData.course) %> -
          <%= String(formData.branch) %></strong
        >
        during the year <strong><%= formData.academicYear %></strong>.
      </p>

      <p class="body-text">
        This Certificate is issued to enable him/her to apply for
        <strong><%= String(formData.certificateFor) %></strong>
        <% if (formData.scholarshipType) { %> (<%=
        String(formData.scholarshipType) %>) <% } %>.
      </p>

      <p class="signature">PRINCIPAL</p>

      <div class="footer-line"></div>
      <p class="footer">
        Ph.No. 0452-2482240-42 &nbsp;&nbsp;&nbsp; Fax No: 0452-2483427
        &nbsp;&nbsp;&nbsp; Email: tceoffice@tce.edu
      </p>
    </div>
  </body>
</html>
